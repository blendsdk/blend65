# Portfolio Roadmap: blend65.ri

> **Status**: Active
> **Last Updated**: 2026-07-29
> **Features**: 0 of 5 done
> **CodeOps Skills Version**: 3.0.0
>
> Per-feature detail — including the full per-slice/per-phase history — lives in each
> feature's own roadmap (linked below) and in the plan directories under
> `codeops/features/<feature>/plans/` and `codeops/_archive/`. This portfolio file keeps
> only the current stage summary per feature.

## Legend

⬜ Backlog · 🔄 In progress · ✅ Done · ⛔ Blocked · ⏸️ Deferred · 📦 Archived

## Features

| Feature | Roadmap | Stage Summary | Progress | Status | Last Updated |
|---------|---------|---------------|----------|--------|--------------|
| compiler-readiness | [→](features/compiler-readiness/00-roadmap.md) | RD-03 done; nine-binding publication selected at `sha256:41557dde…e2706`; historical four-binding authority remains resolvable; exact full verify green | 72/72 RD-03 tasks; 3/8 roadmap RDs | 🔄 | 2026-07-29 |
| game-optimizer-codegen | [→](features/game-optimizer-codegen/00-roadmap.md) | Requirements gate passed: 18 comprehensive RDs and 26 resolved architecture decisions define the C64-first, target-profile-driven whole-program optimizer, expert backend, proof system and commercial game corpus; preflight next | 0/18 | 🔄 | 2026-07-24 |
| blend65-conformance | [→](features/blend65-conformance/00-roadmap.md) | **PROPOSED 2026-07-21** — conformance & expressiveness. A four-lens audit found **four silent miscompiles** (`downto 0` and `0 to NAMED_CONST` both emit branches that can never be taken, so the canonical 6502 countdown and page loops hang; `poke(addr, <word>)` writes two bytes and corrupts the adjacent MMIO register; same-named locals collapse onto one frame slot), plus the reasons a C64 game cannot be written at all: `peek`/`poke` accept only a bare numeric literal — rejecting named constants, literal *expressions*, and the runtime form `spec/12-intrinsics.md:159` **mandates** — so no loop can write screen RAM, colour RAM or a bitmap, and no hardware register can be named (all 18 examples are magic numbers, violating Prime Directive clause 3); there is no screen-code path, so C64 text renders wrong glyphs; there is no way to call an address, so tracker music and KERNAL are unreachable *by construction* and `F015`'s own embedded-SID example rests on a helper the language cannot express; and array-of-struct sprite records ICE six ways. Also 17 dead diagnostic codes (one enforcing a rule the spec explicitly permits), five profile fields read by nothing, a ZP budget reporting 46 bytes against the profile's 142, and `x / 2` emitting `JSR __rt_div8` where the spec promises `LSR` — a cost model **`F017` passed its Language Guard on**. Root cause is instrumentation, not principle: the parity scoreboard measures only programs that compile, so six green RDs were structurally blind, and **six deferrals were orphaned simultaneously** when RD-18 closed over the slices they named as their landing place. 14 items across 10 phases, ordered for correctness of execution; challenger-reviewed, which moved `0 to 255` into the miscompile RD (the named-const spelling is already silent), corrected a sequencing premise that was mechanically wrong (the spill ceiling is `mainTempBytes: 4`, not the ZP budget), reshaped the diagnostics sweep into a permanent registry↔emitter agreement test, and gave #70 and the cast-syntax divergence homes they lacked. Findings: [triage](00-conformance-triage.md) | 0/14 (RD-01 exec 71%) | 🔄 | 2026-07-23 |
| blend65-ri | [→](features/blend65-ri/00-roadmap.md) | **RD-18 ✅ CLOSED 2026-07-17** — frozen v3 compiles end-to-end (unoptimized); next: RD-13/RD-14 (`make_plan`) | 18/20 | 🔄 | 2026-07-17 |
| asm-parity | [→](features/asm-parity/00-roadmap.md) | **RD-15 ✅ DONE 2026-07-21** — alignment granularity, 3 phases / 24 tasks, all 15 acceptance criteria discharged. A const image's boundary follows the arithmetic the source writes (shift 6 → 64, everything else 256, coarsest-wins), so `hi(&X) * 4` stays correct structurally. The deliverable is the bound: `balloon-color` −128 B in a program with no budget, `balloon` unchanged at 318 B, corpus total unmoved, every block image's pad now under 64 (19, 60, 1). Phase 2 review: no findings. Prior: plan preflighted — 10 findings, 0 critical / 1 major, all resolved (the major: three `% 64` alignment guards that could not have failed, plus a Phase 1 that would not have compiled). Plan created same day ([plan](features/asm-parity/plans/rd-15-alignment-granularity/00-index.md) — 3 phases / 24 tasks, gate passed on AR #113–#121; the data-shape migration lands behaviour-neutral ahead of the one behavioural commit, the boundary's legal values are closed at the type so a non-power-of-two `!align` cannot be constructed, and an acceptance clause measured untestable against the real compiler was dropped — both corrections back-propagated in Phase 3. Earlier the same day, RD preflighted — iteration 1: 18 findings, 0 critical / 2 major, all resolved same-day, challenger converged on both majors, fix application queued; drafted earlier the same day — alignment granularity, #69 — gate passed on AR #101–#110 + #111–#112 surfaced during authoring (12 items). A const image is aligned to what the hardware actually reads: the boundary is derived from the divisor the source already writes, so `lo(&X / 64)` demands 64 and a bare `&X` keeps 256, combined **coarsest-wins**, which keeps `hi(&X) * 4` correct *by construction* rather than by caution. Grammar-free — no `spec/` change and no Language-Guard evaluation, so the `@align(n)` attribute RD-03 filed as FUT-014 stays unbuilt and unneeded. The demand set is an **allowlist of hardware granularities, not the divisor**: `lo(&X / 16384)` is a legitimate VIC-bank *read*, not a request to 16 KB-align an image. **#69's figures were stale**, filed before RD-13, and all four address-taken programs were rebuilt to correct them — `balloon-color` is 188 of 584 B and **60 B at 64-byte, not 1**. **The corpus byte delta is 0, and the RD says so in its own acceptance criteria**: `balloon` is the only budgeted address-taken program and it pads 19 bytes under either boundary, so acceptance is invariants plus a bound (pad < 64) and never a byte count — "corpus total decreases" would fail on a *correct* implementation, which is exactly how RD-13 lost AC-8. That bound is the deliverable: it caps byte-ratchet noise at 63 instead of 255, the noise that swallowed RD-13's real 13-byte win whole, so this is a **bound-the-tax RD, not a save-bytes RD** and is written as one. A challenger on a different model family, given five candidate designs **without** the author's preference, converged on the same one and returned three corrections verified against the tree before adoption: the page-alignment oracle inventory is **six, not four** — ST-13f fails deterministically while **ST-C15 and ST-13j pass by luck**, both rounding identically under either boundary, so CI would never flag them and they would surface later disguised as a regression in whatever changed the code size; generalized divisor inference is *unsound*, not merely unclamped; and **RD-13:157-159 predicts this RD makes `hi(&X) * 4` incorrect** — false under the maximum rule and contradicted by RD-13:439 on the same page, corrected here before it reads as pre-authorizing uniform-64. Three of the twelve register items exist only because of that challenger. Next: `preflight` the RD) · **RD-13 ✅ CLOSED 2026-07-21** (symbolic address arithmetic — 54/54 tasks across 5 phases, one commit each. The sprite-pointer idiom went **18 B / 24 cyc → 5 B / 6 cyc**, and `balloon`'s pointer store is now **instruction-for-instruction its hand-written twin** — same 5 bytes, same 6 cycles, the difference being only that the assembler supplies the constant rather than the programmer counting 64-byte blocks by hand, which is the version that cannot go stale when the image moves. That divergence row is **deleted** from the ledger, not re-routed. **The corpus byte total did not move, and that is the headline finding**: the sprite is `!align 256`, so all 13 bytes removed from balloon's code were absorbed by padding growing 6 → 19, leaving the binary at 318 either way. Cycles carry it alone — balloon 300 → **282**, corpus 4237 → **4219** — and AC-8's "corpus total strictly decreases" is recorded as ❌ on bytes / ✅ on cycles rather than reconciled away. **The obvious consolation for that is false and the closeout says so**: `balloon` lands on `$0900` under either alignment, so its pad is 19 bytes either way and **RD-15 recovers none of these 13 bytes on this layout**. The real case for RD-15 is the bound — 0–255 at page alignment against 0–63 at 64 — with `balloon-color` the measured demonstration at **188 B → 60 B** (#69's figures were stale; re-measured at RD-15 authoring). `lo(&X / 64)` now folds to `#<(sym / 64)` instead of `JSR __rt_div16` — a runtime 16-bit software division of a link-time constant — `lo(&X >> 6)` builds instead of erroring, and `W10172` stops firing where the frozen spec forbids it verbatim. All three demos migrated; the two that **no suite had ever compiled** gained builders and suites, emptying the coverage manifest's waiver list. 16 rows re-routed off #58 behind a structural check that no row carries it, written first and watched failing on all 17. The three re-authored balloon rows are the cautionary tale — only one was planned, the review found two more, and *the review's own correction was wrong* because it counted one phase's bytes and not the other's; the committed numbers come from the measurement tool. AR #100 at runtime — a report-parser defect a size coincidence exposed. Spun off **[#73](https://github.com/blendsdk/blend65/issues/73)**. Four reviews on a different model family: no critical, one major. Earlier — plan preflighted 2026-07-21: two iterations, 5-cluster fan-out on a different model family plus two verification auditors: **30 findings, 1 critical + 5 major**, all resolved; now 5 phases / **54 tasks** on AR #88–#99. Design survived intact — zero architectural changes, zero scope creep — and every empirical claim reproduced, so the defects were **all in the oracles**. The critical was a blind spot every scan shared: the plan audited the translate-side consumers of the new operand exhaustively and none audited the lowering-side positions that wrap a result in `const`, where ten sites funnel into a guard that ICEs on it — `let b: byte = lo(&X);` and `v = hi(&X);` compile today and would have regressed **silently** at Phase 2, with Phase 4's own migration target unable to build. Three further majors were assertions that could not fail for the risk they gated: AC-3's slot-counter proof, ST-13j's assembled-byte oracle, and the Prime-Directive hand-review — the last aimed at golden hunks that cannot exist, now aimed at `balloon`'s assembly against its committed hand twin). Previously **T-02 ✅ DONE 2026-07-21** (examples coverage manifest + completeness gate — `examples/` could gain a program verified by nothing at all, and two demos had; a committed manifest now tiers all 18 examples and a spec test asserts the manifest and the directory agree exactly plus each tier's artifact obligations, **including the negative ones** — a `demo` that quietly acquires a golden now fails instead of silently costing every future edit a re-base. Two self-inflicted defects caught during execution and recorded. Spun off [#72](https://github.com/blendsdk/blend65/issues/72) as **RD-16** — a hand-written twin for `boing-ball`, the first genuinely game-shaped parity fixture: the corpus is 14 synthetic slices plus `balloon`, several constant-*by-construction*, so the headline ratio is measured almost entirely against code no game contains. Sequenced **after RD-13**, which moves that program's bytes) · **RD-01 ✅ done** (parity infra) · **RD-02 ✅ done** (#61 — 14-pair twin corpus, twin tier, committed SCOREBOARD.md baseline 4.83×/6.51×, routed audit, CI freshness gate) · **RD-04 ✅ CLOSED 2026-07-19** (#50 — compare-and-branch fusion, 5 phases/43 tasks; conditions branch on their comparison's flags, `!` is a free label swap, `&&`/`||` are CFG edges claiming no slot; corpus 4172→3896 B / 5340→5023 cyc, raster poll at 12 cycles, AC-1…AC-10 walked, #50 divergence rows gone from every pair; spun off #66) · **RD-05 🔬 PLAN PREFLIGHTED 2026-07-20** (#51 — block layout: fall-through elision, jump threading, unreachable-block removal, branch inversion, plus #65 branch-range relaxation; RD gate passed on 8 items, RD preflight raised 32 findings / 10 major. Plan: 5 phases / **58 tasks**, build-unwired-then-wire so the corpus regenerates exactly once, relaxation wired first as a provable no-op; plan gate added AR #34–#39. Plan preflight — 5 clusters + a hardening challenger — raised **27 findings / 6 major**, all applied as AR #40–#57: the sole proof of AC-7 sat in a package that cannot reach the `--optimize` gate; every program's `bytes` ratchet would have gone silently slack, costing `balloon` its only size gate; the permanent corpus scan saw only the missed-*elision* half of the tail decision, and the challenger caught that the obvious third invariant is textually identical to relaxation's own emitted form, so it ships with an `_rlx<N>` carve-out. Three findings were RD defects and were back-propagated — AC-10, AC-13 and a re-anchoring hazard rationale the observable tables refute. Architecture unchanged; scope creep zero; verify green) · **RD-05 ✅ CLOSED 2026-07-20** (#51 — block layout, 5 phases / 58 tasks, one commit per phase, corpus regenerated exactly once: trailing jumps elide into fall-through, branches invert when the true edge follows, trampoline chains thread through, unreachable blocks are not assembled. Corpus 4172→3896→**3616 B** and 5340→5023→**4724 cyc**; 4.23×→3.93× vs the twins. `rasterpoll`'s poll loop is now its twin instruction for instruction and its routing moved off #51; **#65 closed** — out-of-reach branches relax to the hand-written branch-over-jump form at a fixpoint. Four permanent corpus invariants, each seeded and watched to fail, with the `_rlx<N>` carve-out verified both ways. AR #58–#63 at execution; four reviews, no major findings) · **RD-03 ✅ CLOSED 2026-07-20** (#49 placement slice — a const array whose address is taken is page-aligned, so the VIC reads the embedded sprite where it already sits. Grammar-free: no `spec/` change, no Language-Guard evaluation, and the earlier "blocked on copy()/v3.1" framing was refuted by measurement. RD preflight raised 29 findings / 2 critical / 7 major, all resolved — the trigger rule was undefined at the level that matters, since `&X` and an ordinary by-reference array argument emit the *same* IL `addrOf` operand, so an IL scan would have aligned `slice7b`/`slice8b` (+435 B); M1 is now the **syntactic** `&` set. Plan: 5 phases / **41 tasks**, mechanism-first so the 14 byte-identical goldens are a free proof the rule excludes by-ref arguments, with 7 decisions as AR #69–#75. Plan preflight — 5 clusters + a hardening challenger — raised **28 findings / 0 critical / 5 major**, all resolved and applied. Design unchanged, scope creep **zero**, and every empirical claim reproduced independently first (677 / 312 / **318 B**, `$0A67` / `$08FA` / **`$0900`**, sprite block 36, and all four rows of the ACME `!align` bitmask trap table on real ACME 0.97). Load-bearing catches: the marking set was described against **one of two** `LowerCtx` sites, so a module-scope `&` — confirmed live by compiling a probe — would never align while the whole planned suite passed; Phase 4 was CI-red by construction because the hard-fail scoreboard freshness gate rebuilds from `examples/` source while the ratchets sat a commit later, the root cause being a Verify command strictly weaker than CI; and two `[CI]` criteria rested on tests that would have *skipped* in CI rather than failed, or that cannot fail at all. Four RD defects back-propagated, and AC-9 relabelled `[CI]` → `[Review]` — CI has no `spec/` freeze step). **Executed**: 41/41 tasks, one commit per phase. balloon 677→**318 B**, 2.70×→**1.27×** — its sprite is page-aligned at `$0900` (block 36) and read in place, with the 63-store staging copy gone entirely; the compiler now beats the twin at *runtime* and stays behind it on bytes, and both halves are stated. Corpus 3616→**3257 B** / 4724→**4237 cyc**, 3.93×→**3.54×**; every other fixture unchanged to the byte, all 14 goldens byte-identical across the range, `spec/` untouched in all 7 commits. The mechanism landed first as a provable no-op, so the byte-exact goldens were a free proof that the rule excludes by-reference arguments — the RD preflight's most dangerous finding. Three claims were seeded and watched to fail rather than asserted: `!align 256, 0` (build succeeds, directive present and correctly placed — only the resolved-address oracle notices nothing moved), the VICE pointer check, and the staging-store check, whose first draft used padded hex the serializer never emits and would have passed vacuously. All four twins.json routing rows were false once the copy vanished and were re-authored from measurement; a **data placement** divergence group appeared for the first time and is routed. Residual 67 B = +61 code (→#52/#58) + **6 B page padding**, an accident of where the image lands. Two runtime ARs (#76–#77), the second from a preflight gap — the hazard was identified but the file sweep missed `gen-parity-scoreboard.spec.test.ts`. Four reviews on a different model family: no critical, **one major** (a debug probe committed by accident), rest minor. The [area report](https://github.com/blendsdk/blend65/issues/49#issuecomment-5024737189) was posted 2026-07-20 and retracts the issue's own 2026-07-18 framing — "`copy()` is required for the balloon, and placement can't substitute" — whose premise (`$0340` is below the load base) was true and whose conclusion was not: the twin's author *chose* a block there. **#49 stays open** — placement slice only · **#69 filed as RD-15 2026-07-20** (an external review of the `balloon-color` demo's assembly, analysed against the register: most findings were already owned or already shipped, and four of nine grade the compiler for things it does correctly — MMIO store ordering and `poke` as a raw store. Two were new. Alignment granularity is 256 bytes where a C64 sprite needs 64 — **193 of 584 bytes (33%) padding** on that demo, 1 byte at 64-byte alignment. The defect is not the constant: `hi(addr) * 4 === addr / 64` holds only at page alignment, `&X / 64` compiles to a runtime `JSR __rt_div16` dividing a link-time constant, and `&X >> 6` is rejected — so the one expressible sprite-block idiom *requires* the alignment that costs the bytes. Blocked on RD-13/#58's symbolic fold. Also unsized until now: 48 absolute frame-variable accesses where zero page saves a byte and a cycle each, **−48 B**, strengthening #59. `--optimize` confirmed a no-op today — it gates only the empty peephole catalog. Two fixes committed: the demo enabled `$D015` before writing coordinates, and three translator diagnostics leaked an internal planning ID to the user's terminal) · **RD-13 📋 PLAN CREATED 2026-07-21** (#58 symbolic-address slice — plan: 5 phases / **52 tasks**, gate passed on AR #88–#96. Order is **M3 → M1 → M2-unwired → migrate → ledgers**, so the corpus regenerates exactly twice and no intermediate commit ships the fixed four-instruction sprite-pointer idiom while still warning that it generates a shift-and-add sequence. The load-bearing decision — how a byte-selected link-time address is carried through IL and instruction selection — went to an independent challenger on a different model family **before** a preference was recorded, and it converged on the same answer for the same reason: **distinct variants in both unions** (`addrByte`, and a no-offset `symbolExpr`), because extending the existing variants fails *silently* at the two guards that already **accept** an address — `translateStore` would emit the two-byte marshalling pair for a byte-typed value, writing one byte past a byte slot, and `rightSource` derives the byte select from the byte index, so a high-select read at index 0 emits `#<sym`; both assemble cleanly, whereas a distinct kind is TS2366-forced at both serializers and leaves all seven `isAddr` guards untouched. The challenger additionally found **three pre-existing silent-failure holes sitting on the new operand's exact path** — `leftIntoA` falls out of its if-chain emitting nothing, so a later `STA` stores whatever stale value sits in A; `bringValueIntoRegisters` has an `if (lo && hi)` with no else; `rightSource` falls through to a bare `Implied` operand only ACME notices — all unreachable today and closed with ICE guards in Phase 2 *before* the operand exists, so full-verify-green at that point **is** the unreachability proof. Grounding found two RD defects, scheduled for back-propagation: the RD's claim that a local's `&` is "already excluded upstream" is false (`lowerAddressOf` resolves a local to a `__frame_*` symbol and `emitHi`/`emitLo` reach it through the same branch, so M1 is uniform across all four kinds), and M1 needs **no** new legal IL position at all, since `emitLo`/`emitHi` return the operand directly as they already do for a numeric literal — `addr`'s two-position rule stands. Also settled: `k = 1..15` with `k = 0` degenerating and everything else falling through with **no new diagnostic**; a **named const** divisor folds, because refusing it makes the more readable spelling the one that emits `JSR __rt_div16`; and `balloon-color` — grep-confirmed referenced by **nothing** in `packages/`, `test/`, `scripts/` or `.github/` — gets its first CI signal ever as a build-only symbol-map check, staying outside the corpus as its own header asks. A third migration site surfaced after the plan was written: the newly committed `examples/boing-ball` carries the same idiom in a **`let` initializer**, and it is the strongest of the three — its ball is four consecutive 64-byte blocks addressed as `base+0..3`, so the program already does 64-byte block arithmetic while naming its first block through a page-alignment identity that holds only by luck, and it is the demo RD-15 breaks first. AC-6 now migrates **all three** (AR #96). Phase 3 lands the fold **unwired**, making the 14 byte-identical goldens a free proof it changed nothing it was not asked to. Earlier — gate passed on AR #78–#82; a 5-cluster preflight fan-out on a different model family then raised **27 findings (0 critical, 8 major)**, all resolved as AR #83–#87, spinning off [#70](https://github.com/blendsdk/blend65/issues/70) (local constant propagation — the destination for 16 misrouted divergence rows, and on the numbers the largest remaining parity gap on the board) and [#71](https://github.com/blendsdk/blend65/issues/71) (OP-5 strength reduction — `W10172`'s future producer). The architecture survived intact: every central risk claim held under adversarial verification, several reproduced with the repo's own toolchain. The defects were all in test accounting and boundary precision — **the spec-test blast radius was understated three-fold** (`ST-9b` pins the exact homing M1 abolishes, `ST-C14` pins the instruction run the idiom migration deletes, so the RD's bolded "the one place RD-13 touches a spec test" was false); **AC-3 could not fail**, naming a frontend suite with no `&` fixture that cannot observe codegen's counter — the RD-03 preflight's own failure mode reproduced; and **the new operand's rendering silently miscompiled**, since `#<(sprite+3 / 64)` assembles cleanly to `0x00` (ACME binds `/` tighter than `+`), so the offset field was dropped and the trap made unreachable by construction. The two most serious findings were raised by two clusters independently. Detail: An address is a link-time constant ACME folds for free; the compiler materializes it at runtime, and three legal v3.0 forms fail three ways, all measured live: `hi(&X) * 4` costs 8 instructions plus a spurious `W10172`, `lo(&X / 64)` emits 11 instructions and `JSR __rt_div16` — a runtime 16-bit software division of a link-time constant — and `lo(&X >> 6)` is rejected with `E90001`. The RD makes the byte-selects one instruction, folds the divide/shift forms to `#<(sym / 64)` behind one **restricted** `InstrOperand` variant, and stops `W10172` firing where `spec/evaluations/F017-operators.md:442` forbids it verbatim. Load-bearing catches from authoring: the seam **must** route through `lowerAddressOf`, because that function also carries RD-03's alignment mark and `examples/balloon`'s only `&` is this exact site — bypassing it silently unaligns the sprite with nothing in CI able to notice, since balloon has no golden; the variant's blast radius is exactly **one** compiler-forced site (`symbolText`'s explicit `: string` return makes a missing arm a hard TS2366 — an earlier claim that it would render `undefined` silently was wrong and is corrected); and **16 of the 17 corpus rows routed to #58 are misrouted**, being local constant-propagation gaps in codegen dataflow that drive the corpus's worst ratios (`slice6` 8.70×, `slice3b` 8.32×) — so RD-13 moves **1** routed row, re-routes the other 16, and the largest remaining parity gap turns out to be filed under the wrong owner. A challenger on a different model family endorsed the scope and corrected three more: don't full-fold `hi(&X) * 4` because its mod-256 wrap is *meaningful* — bless `lo(&X / 64)` as the sprite-block idiom instead; `W10172` is left deliberately producer-less, since the spec's positive case has no implementation at all; and M1 is not shippable without M3. `const` declarations naming link-time addresses excluded as **spec-blocked under D3**, not merely expensive. Next: `preflight` the plan) | 6/16 RDs | 🔄 | 2026-07-21 |

## Archived

| Feature | Roadmap | Completed | Last Updated |
|---------|---------|-----------|--------------|
| — | — | — | — |

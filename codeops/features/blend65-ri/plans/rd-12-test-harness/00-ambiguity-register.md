# Ambiguity Register: RD-12 — Test Harness & Emulator Verification

> **Status**: ✅ GATE PASSED — all 17 items resolved (2026-07-03)
> **Last Updated**: 2026-07-03
> **Feature**: blend65-ri · **Implements**: RD-12
>
> **Prefix convention.** Plan-local items use the `AR-H<n>` prefix ("H" = harness)
> to avoid collision with the RD-15 plan register (`AR-V<n>`) and the requirements
> register (`AR-P<n>`). Where an item resolves a requirements-level `PF-NN` preflight
> finding or an RD `R<n>`, that source is cited in the row.
>
> **Resolution provenance.** H1–H7 were presented as three clarifying-question
> rounds and resolved by explicit per-question selection (scope; compiler
> dependency; verify bar; screenshots; AC-14 vectors; test isolation; DEF-2
> disposition). H8–H11 were presented with the scope confirmation as
> "engineering-mechanic recommendations for veto" and ratified by the user's
> **"Proceed"** (2026-07-03). H12–H17 are naming/layout items with a single viable
> grounded answer (obvious-fact tier) recorded for a complete audit trail.
>
> **Grounding note.** Every technical row was verified against the live tree
> (`x64sc` = VICE 3.10 and `acme` both on `PATH`; a real `build()` of
> `examples/gate/main.blend` was executed to pin symbols and to surface DEF-2).
> Nothing in this register is speculative.

| # | Category | Ambiguity / Gap | Options Presented | User Decision | Status |
|---|----------|-----------------|-------------------|---------------|--------|
| H1 | Scope | How much of RD-12's 16 ACs does this plan cover? | (a) full RD, phased; (b) MVP core slice deferring golden helper (AC-10) + screenshots (AC-09) | **(a) Full RD, phased.** VICE 3.10 + ACME are both installed locally, so every tier is buildable now; no AC is blocked. Deferring would leave RD-12 partially open across a cycle for no capability gain | ✅ Resolved |
| H2 | Integration | How does `@blend65/test-harness` obtain the compiler's `BuildResult` type and parse the VICE label file? Today it depends on `@blend65/core` only; R27/R28 name compiler symbols | (a) depend on `@blend65/compiler`, reuse `parseLabelFile` + import type `BuildResult`; (b) stay lean — structural `{symbolMap?,binaryPath?,binary?}` shape + a re-implemented label parser, compiler dev-only | **(a) depend on compiler; reuse.** Both `parseLabelFile` and `BuildResult` are already public in the compiler barrel (`compiler/src/index.ts:30,36`); DRY (one label parser). R15/AR-20 forbids only frontend/language-server→codegen, so test-harness→compiler is clean. Cost accepted: the published harness pulls in `@blend65/compiler` | ✅ Resolved |
| H3 | Testing | Emulator + RD-17 AC-14 tests are local-only (AR-27 keeps them out of CI). What is the completion bar for the ACs they cover? | (a) prove locally on VICE (skipIf keeps CI green, executor must run green locally before ticking); (b) build machinery, CI-skip only | **(a) prove locally on VICE.** Author with `describe.skipIf(no VICE)` (mirrors the shipped `runtime-asm.spec.test.ts:56` skipIf-ACME pattern → AC-13 graceful skip); the executor MUST run them against the installed VICE 3.10 and confirm green before ticking AC-02/03/04/05/13 and the RD-17 AC-14 discharge. VICE is present, so the emulator path is proven, not merely built | ✅ Resolved |
| H4 | Technical | `captureScreenshot()` (AC-09, failure artifact only — never golden-matched) must turn VICE's `DISPLAY_GET` output into a saved file "as PNG" | (a) hand-rolled zero-dep PNG (Node `zlib`); (b) save raw display buffer as-is; (c) add a PNG library (pngjs) | **(a) hand-rolled zero-dep PNG.** Honors the RD's literal "saved as PNG" with no new dependency (consistent with the AR-V2 zero-dep ethos). Encode `DISPLAY_GET`'s indexed buffer + `PALETTE_GET` palette to a truecolor PNG via `zlib.deflateSync` (stored/deflate). ~50–80 LOC. (c) rejected: first non-`@blend65` runtime dep for a debug-only artifact | ✅ Resolved |
| H5 | Testing | RD-17 AC-14 re-verifies `__rt_mul8/mul16/div8/div16` on VICE. The interim interpreter test already runs exhaustive edge crosses + 500 seeded-random/routine. What coverage should the VICE-tier vectors carry? | (a) bounded subset (all edge crosses + ~25 seeded-random/routine); (b) full parity (~2000+ vectors) | **(a) bounded subset.** Each vector is one in-session monitor round-trip (`REGISTERS_SET`/`MEMORY_SET` → `EXECUTE_UNTIL_RETURN` → read back); a bounded set proves real-silicon parity while keeping the local-only suite quick. The interpreter test retains the exhaustive 500-sample coverage (it is faster, in-process). Discharges the *inherited* RD-17 AC-14 (distinct from RD-12's own AC-14 = publishable package) | ✅ Resolved |
| H6 | Behavioral | How does the fixture isolate one whole-program test's binary from the next? | (a) relaunch VICE per binary with `-autostart <prg>`; (b) one session + `RESET`/`AUTOSTART` between binaries | **(a) relaunch VICE per binary.** `beforeAll`/`afterAll` owns a VICE process per test file; each distinct binary gets a clean VICE launched with `-autostart <prg>` (matches RD §4.5). Deterministic, no residual state; the tier is local-only so process-spawn wall-time is acceptable for MVP. **Nuance:** routine-vector (AC-14) tests load the single routine binary once and inject *per-vector* via `MEMORY_SET`/`REGISTERS_SET` + `EXECUTE_UNTIL_RETURN` — no relaunch between vectors (H5) | ✅ Resolved |
| H7 | Integration (runtime) | **RD-09 defect surfaced by a live gate build (DEF-2):** `invokeAcme` passes `-l <path>` (ACME's `--symbollist`, the native `name = $addr` format), but `parseLabelFile`'s regex only matches the VICE format `al C:xxxx .name`. Every real `build()` therefore returns an **empty `symbolMap`** (verified: gate build → `symbolMap.size === 0`), so RD-12's `runUntilLabel` + symbolic `assertMemory` (AC-03/AC-08, R19/R20/R28) have no symbols. ACME's actual VICE-format flag is `--vicelabels`. Slipped through because RD-09's `parseLabelFile` unit tests feed synthetic VICE text and RD-15's real-ACME E2E never asserted `symbolMap` contents | (a) fix inside the RD-12 plan (Phase 0); (b) standalone RD-09 bugfix task first | **(a) fix in-plan (Phase 0).** Change `invoke-acme.ts` `acmeArgv` `-l` → `--vicelabels` (verified live: `--vicelabels` emits `al C:0819 ._main`, `al C:080d .__startup` — exactly `parseLabelFile`'s regex). Add a regression **spec** test asserting a real gate build yields a non-empty `symbolMap` containing `_main`/`__startup`; update the RD-09 `invoke-acme.impl.test.ts` argv assertion (impl test, not an immutable oracle). Direct precedent: RD-15 fixed the RD-09 DEF-1 header defect in-plan (AR-V23). Tracked as roadmap **DEF-2** | ✅ Resolved |
| H8 | Technical | Default VICE binary-monitor TCP port | (a) 6502 (RD R10 literal), overridable; (b) another default | **(a) 6502**, overridable via `LaunchOptions.monitorPort`. Matches RD §4.5's `-binarymonitoraddress 127.0.0.1:6502`. Bind loopback only (`127.0.0.1`) — never a routable interface | ✅ Resolved (veto-open, ratified) |
| H9 | Behavioral | Gate-program sync point — R20 names `__startup_done`/`_main` epilogue, but the live build emits **no** such label (only `_main=$0819`, `__startup=$080d`; verified) | (a) `runUntilMemory(0xD020,5)` primary + `runUntilLabel('_main')` for AC-03; (b) add a codegen `__startup_done` label (RD-07c change) | **(a).** `runUntilMemory(0xD020,5)` directly proves the `poke` (the gate's whole point) and needs no epilogue label; `runUntilLabel('_main')` exercises AC-03 against a real emitted symbol; symbolic `assertMemory` is demonstrated by asserting the byte at symbol `_main` equals the first opcode `LDA #$05` = `$A9`. (b) rejected: mutating RD-07c codegen is out of RD-12 scope. **PF-004 compliance:** all symbol spellings are pinned against real ACME output, not assumed | ✅ Resolved (veto-open, ratified; PF-004) |
| H10 | UX | How is `--update-golden` (R31) surfaced in a Vitest run? | (a) `UPDATE_GOLDEN` env var; (b) a CLI flag; (c) vitest config toggle | **(a) `UPDATE_GOLDEN=1 yarn test`.** Idiomatic for Vitest (no arg-parser plumbing, no runner coupling); `assertGolden` reads `process.env.UPDATE_GOLDEN` and, when set, writes the golden file instead of asserting. Documented as a manual developer action (R31) | ✅ Resolved (veto-open, ratified) |
| H11 | Architecture | Runtime-dependency budget for the harness | (a) zero runtime deps beyond `@blend65/*` — Node built-ins `net` (monitor socket) + `zlib` (PNG); (b) allow small deps | **(a) zero non-`@blend65` runtime deps.** VICE monitor transport uses Node's built-in `net`; PNG uses built-in `zlib`; process spawn uses `node:child_process`. Consistent with AR-V2. The new `@blend65` edges are `@blend65/compiler` (**runtime** — `BuildResult`/`parseLabelFile`, H2) and `@blend65/codegen` (**test-scope devDependency** — `loadRuntimeModule` for the RD-17 routine vectors, see 03-04). Both are workspace packages, so the zero-third-party-dependency budget holds. *(PF-001 correction: the original "the only new dep is compiler" phrasing omitted the test-scope codegen edge that the RD-17 vectors require.)* | ✅ Resolved (veto-open, ratified; PF-001) |
| H12 | Naming | Plan folder name | `rd-12-test-harness` / alternatives | `rd-12-test-harness` (mirrors the RD filename `RD-12-test-harness.md`; consistent with `rd-16-compiler-configuration`) | ✅ Resolved |
| H13 | Technical | `@blend65/test-harness` `src/` module layout | proposed layout vs adjust | **Adopted as proposed.** `src/emulator/driver.ts` (interface + types), `src/emulator/vice/protocol.ts` (pure codec), `src/emulator/vice/vice-driver.ts` (socket transport + `ViceDriver`), `src/emulator/vice/png.ts` (screenshot encoder), `src/emulator/registry.ts` (R7a platform→emulator table), `src/run/strategies.ts`, `src/run/assertions.ts`, `src/fixture.ts` (`setupEmulator`), `src/golden.ts` (`assertGolden`), `src/index.ts` (public barrel). Each file < ~500 LOC (coding standard) | ✅ Resolved |
| H14 | Technical | Is the VICE binary-monitor wire protocol testable in CI (no VICE)? | (a) split a pure codec (frame encode/decode) from socket transport; (b) monolithic driver, all tests skipIf-VICE | **(a) split.** `protocol.ts` is a pure `Uint8Array`↔structured-command codec with **no** I/O; its spec tests run in CI (byte-exact framing per the VICE 3.7+ spec). The socket-bound `ViceDriver` integration tests are skipIf-VICE (H3). Maximizes CI-verified surface for a local-only tier | ✅ Resolved |
| H15 | Technical | VICE `REGISTERS_GET`/`REGISTERS_SET` use numeric register **ids** that are not fixed across builds; the RD's `Registers` type uses names (a/x/y/sp/pc/flags) | (a) query `REGISTERS_AVAILABLE` at connect, build a name→id map; (b) hardcode ids | **(a) query `REGISTERS_AVAILABLE`.** On `launch()`/connect the driver issues `REGISTERS_AVAILABLE` (0x83) and builds the `name→id` map, then maps to/from the RD's `Registers` shape. Robust across VICE versions; the exact ids are validated live during exec | ✅ Resolved |
| H16 | Naming | The RD `Registers.flags` type spells the break flag `break_` (`break` is a reserved word) | keep `break_` / rename | **Keep `break_`** exactly as the RD §4.1 interface specifies (the RD already worked around the reserved word). No divergence from the published contract | ✅ Resolved |
| H17 | Documentation | The CLAUDE.md package dependency table lists `test-harness → core`; H2 adds `compiler` (+ PF-001 adds a test-scope `codegen` edge) | update table / leave stale | **Update** the CLAUDE.md dependency-table row to `@blend65/test-harness → core, compiler` *(+ `codegen` dev-only, RD-17 vectors)* as a Phase-3 bookkeeping task, and add the paired `packages/test-harness/tsconfig.json` project references (`../compiler`, `../codegen`) per the repo's 1:1 deps↔references convention (`cli/tsconfig.json` precedent). Keeps the load-bearing R15 table accurate | ✅ Resolved (PF-001) |

### Resolution Notes

- **No CRITICAL/MAJOR ambiguity remained** after the three question rounds; H8–H11 were
  surfaced for veto with the scope confirmation and ratified. The advisor tool was
  unavailable this session; grounding was done by direct live probes (VICE version, ACME
  flags, a real gate build) rather than a challenger.
- **DEF-2 (H7)** is the one cross-RD code change. It is a one-flag fix with a live-verified
  outcome and a new regression oracle; it is sequenced as Phase 0 so every later phase can
  rely on a populated `symbolMap`.
- **Traceability:** every decision in the component docs and testing strategy cites the
  `AR-H<n>` (or `R<n>`/`PF-NN`/`AC-NN`) that resolved it.

// @fixture: edge-cases/deeply-nested/deep-loops
// @category: edge-cases
// @description: Tests compiler handling of deeply nested loop structures
// @expect: success
// @output-contains: JMP
// @tags: edge-cases, nested, loops

module DeepLoops;

function main(): void {
    let total: byte = 0;
    
    // 2 levels of nested for loops
    for (a = 0 to 3) {
        for (b = 0 to 3) {
            total = total + 1;
        }
    }
    
    // Total should be 4 * 4 = 16
    poke($0400, total);
}
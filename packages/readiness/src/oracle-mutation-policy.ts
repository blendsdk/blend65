/**
 * Maximum byte length of each authority artifact used by mutation-suite hydration.
 *
 * @example
 * ```ts
 * if (bytes.byteLength > ORACLE_MUTATION_AUTHORITY_MAX_BYTES) return;
 * ```
 */
export const ORACLE_MUTATION_AUTHORITY_MAX_BYTES = 16_777_216;

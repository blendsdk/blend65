// @fixture: parser/expressions/unary/unary-minus
// @category: parser
// @description: Tests parsing of unary minus operator
// @expect: success
// @output-contains: LDA
// @tags: expressions, unary, negation

module UnaryMinus;

function main(): void {
    // Basic unary minus with literal
    let negLiteral: byte = -5;
    
    // Unary minus with variable
    let a: byte = 10;
    let neg: byte = -a;
    
    // Double negation
    let doubleNeg: byte = --a;
    
    // Unary minus in expression
    let result: byte = 10 + -5;
    
    // Unary minus with parentheses
    let parenNeg: byte = -(a + 5);
    
    // Negation in arithmetic
    let computed: byte = 20 - -a;
    
    // Store results
    poke($0400, neg);
    poke($0401, result);
    poke($0402, parenNeg);
}
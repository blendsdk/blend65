### Build, Test, and Terminal

1. Allways prefix shell command withs `clear &&` to clear the terminal
2. For building and testing all packages run from package root `yarn clean && yarn build && yarn test`

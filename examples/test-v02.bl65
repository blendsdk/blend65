enum GameState
  MENU = 0,
  PLAYING,
  PAUSED,
  GAME_OVER
end enum

function gameLoop(): void
  while true
    for i = 0 to 10
      if i == 5 then
        break
      end if
      if i == 3 then
        continue
      end if
    next i
  end while
end function
